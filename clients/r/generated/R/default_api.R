#' iplocation.net API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 0.9.0-pre.0
#' Contact: blah@cliffano.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Default operations
#' @description DefaultApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ RootGet } \emph{ Get geolocation of an IP address }
#' Retrieve geolocation of an IP address.
#'
#' \itemize{
#' \item \emph{ @param } ip \link{AnyType}
#' \item \emph{ @param } format \link{AnyType}
#' \item \emph{ @param } delimiter \link{AnyType}
#' \item \emph{ @returnType } \link{Get200Response} \cr
#'
#'
#' \item status code : 200 | Successfully processed the request.
#'
#' \item return type : Get200Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Failed to complete the request.
#'
#' \item return type : Get400Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Command not found.
#'
#' \item return type : Get400Response
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  RootGet  ####################
#'
#' library(openapi)
#' var_ip <- TODO # AnyType | An IPv4 or IPv6 address that you would like to lookup.
#' var_format <- TODO # AnyType | Output format, the following formats are supported: plain xml json jsonp php csv serialized (Optional)
#' var_delimiter <- TODO # AnyType | Delimiter between proxies. Can be used only with format plain. The following types are supported: 1 for \"\\n\", 2 for \"<br>\". (Optional)
#'
#' #Get geolocation of an IP address
#' api_instance <- DefaultApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RootGet(var_ip, format = var_format, delimiter = var_delimiterdata_file = "result.txt")
#' result <- api_instance$RootGet(var_ip, format = var_format, delimiter = var_delimiter)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DefaultApi <- R6::R6Class(
  "DefaultApi",
  public = list(
    api_client = NULL,
    #' Initialize a new DefaultApi.
    #'
    #' @description
    #' Initialize a new DefaultApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Get geolocation of an IP address
    #'
    #' @description
    #' Get geolocation of an IP address
    #'
    #' @param ip An IPv4 or IPv6 address that you would like to lookup.
    #' @param format (optional) Output format, the following formats are supported: plain xml json jsonp php csv serialized
    #' @param delimiter (optional) Delimiter between proxies. Can be used only with format plain. The following types are supported: 1 for \"\\n\", 2 for \"<br>\".
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Get200Response
    #' @export
    RootGet = function(ip, format = NULL, delimiter = NULL, data_file = NULL, ...) {
      local_var_response <- self$RootGetWithHttpInfo(ip, format, delimiter, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get geolocation of an IP address
    #'
    #' @description
    #' Get geolocation of an IP address
    #'
    #' @param ip An IPv4 or IPv6 address that you would like to lookup.
    #' @param format (optional) Output format, the following formats are supported: plain xml json jsonp php csv serialized
    #' @param delimiter (optional) Delimiter between proxies. Can be used only with format plain. The following types are supported: 1 for \"\\n\", 2 for \"<br>\".
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Get200Response) with additional information such as HTTP status code, headers
    #' @export
    RootGetWithHttpInfo = function(ip, format = NULL, delimiter = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`ip`)) {
        stop("Missing required parameter `ip`.")
      }




      query_params[["ip"]] <- `ip`

      query_params[["format"]] <- `format`

      query_params[["delimiter"]] <- `delimiter`

      local_var_url_path <- "/"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "Get200Response", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
